
export declare interface GameSetting {
  normalGamePrice: number;
  rubberGamePrice: number;
  ballPrice: number;
}

export declare interface Player {
  name: string;
  normalGame: number;
  rubberGame: number;
  ball: number;
  paid: boolean;
}